# ============================================
# MacNotes Application Configuration
# ============================================
# Default Development Profile
# ============================================

# Application Name
spring.application.name=backend

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5437/postgres}
spring.datasource.username=umerqureshi
spring.datasource.password=
# spring.datasource.driver-class-name=org.postgresql.Driver   # optional

# ============================================
# JPA/Hibernate Configuration
# ============================================
spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.show-sql=false
#spring.jpa.properties.hibernate.format_sql=true

# ============================================
# File Upload Configuration
# ============================================
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# ============================================
# Server Configuration
# ============================================
#server.port=8080

# ============================================
# Logging Configuration
# ============================================
#logging.level.root=INFO
#logging.level.com.example=DEBUG
#logging.level.org.hibernate=DEBUG


# ============================================
# Production Profile
# spring.profiles.active=prod
# ============================================
# To use production profile, add:
# spring.profiles.active=prod
# And set environment variables:
# DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD, SERVER_PORT

# Production Database Configuration (using environment variables)
# spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:kamals19_db}
# spring.datasource.username=${DB_USER}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Production JPA Configuration
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Production Server Configuration
# server.port=${SERVER_PORT:8080}
# server.servlet.context-path=/server

# Production Logging Configuration
# logging.level.root=WARN
# logging.level.com.example=INFO